#!/usr/bin/env python

import sys
import salpytools
if __name__ == "__main__":
    commands = sys.argv[1:]
    if len(commands) < 1:
        commands = ['disable', 'standby', 'exitControl']
        print("Will execute Power-down Sequence:")
    salpytools.command_sequencer(commands, Device='ATHeaderService')
