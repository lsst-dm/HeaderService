#!/usr/bin/env python

import sys
import HeaderService.SAL_tools_send as SAL_tools_send

if __name__ == "__main__":
    try: 
        commands = sys.argv[1:]
    except:
        commands = ['Disable','Standby','ExitControl']
    SAL_tools_send.ocs_sequencer(commands)
